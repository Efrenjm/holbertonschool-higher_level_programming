// #!/usr/bin/python3
// """defines a square with size"""


// class Square:
//     """a square class"""

//     def __init__(self, size):
//         self.__size = size
    
    
InitInput = {"SelectedDate":0}
EndInput = {"SelectedDate":0}
TimeUnit = {"Days": ""}
function DateDiff(init,end,format){}
function Weekday(init){}

DateDiff(InitInput.SelectedDate, EndInput.SelectedDate, TimeUnit.Days)

Weekday(InitInput.SelectedDate)
if (Weekday(EndInput.SelectedDate) <= 5; )



/*Else if*/ Weekday(EndInput.SelectedDate; 2) <= 5; 
EndInput.SelectedDate - InitInput.SelectedDate;
/*Else*/
EndInput.SelectedDate - InitInput.SelectedDate + Weekday(InitInput.SelectedDate; 2))
//+ Weekday(InitInput.SelectedDate; 2)
//- If(Weekday(EndInput.SelectedDate; 2)<=5; 0; Weekday(EndInput.SelectedDate; 2)-5)